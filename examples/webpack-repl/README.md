# squint webpack browser REPL

Browser nREPL without vite: the squint `SquintPlugin` runs the nREPL + WS
servers inside `webpack serve`.

```
npm install
npm run dev
```

Connect an editor to nREPL port 1341 and eval in the page at
http://localhost:5299.
